---
const { imgSrc, title, description, altText } = Astro.props;
---

<div class="flex-1 bg-white rounded-lg overflow-hidden shadow-md hover:shadow-lg transition duration-300">
  <img
    src={imgSrc}
    alt={altText}
    class="w-full h-48 object-cover"
    loading="lazy"
  />
  <div class="p-4">
    <h4 class="text-xl font-semibold text-[#8B4513] mb-2">{title}</h4>
    <p class="text-[#A0522D]">{description}</p>
  </div>
</div>
